extends ../layouts/main-template
include ../components/blog-list-template

block vars
    - const header = true;
    - const footer = true;
    - var currentUrl = 'blog-no-sidebar.html';

block title
    title Blog Full Width — Blackgrow

block description
    meta(name='description', content='Blog Full Width — Blackgrow.')

block content
    +blogListTemplate('none')
