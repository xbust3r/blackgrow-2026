extends ../layouts/main-template
include ../components/blog-list-template

block vars
    - const header = true;
    - const footer = true;
    - var currentUrl = 'blog.html';

block title
    title Blog — Blackgrow

block description
    meta(name='description', content='Blog — Blackgrow.')

block content
    +blogListTemplate('right')
