extends ../layouts/main-template

block vars
    - var header = true;
    - var footer = true;
    - var currentUrl = 'kids-store.html';

block title
    title Kids Store — Blackgrow

block description
    meta(name='description', content='Kids Store — Blackgrow.')

block content
    include ../components/heros
    +photoHero('photo_hero', 'h1')
    include ../components/icon-with-text
    include ../components/products-cards-4
    include ../components/customers-reviews
    include ../components/categories
