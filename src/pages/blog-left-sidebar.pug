extends ../layouts/main-template
include ../components/blog-list-template

block vars
    - const header = true;
    - const footer = true;

block title
    title Blog Left Sidebar — Blackgrow

block description
    meta(name='description', content='Blog Left Sidebar — Blackgrow.')

block content
    +blogListTemplate('left')
